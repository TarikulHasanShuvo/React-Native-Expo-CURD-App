import React from 'react';
import {Layout, Text} from "@ui-kitten/components";

const Profile = () => {
    return (
        <Layout>
            <Text>Profile</Text>
        </Layout>
    );
};

export default Profile;